<div class="basket-box">
  <% cart.basket_items.each do |basket_item| %>
    <div class="basket-item">
      <div class="remove-button">
        <a id="no-no-bootstrap">x</a>
      </div>
      <div class="d-flex">
        <p><%= basket_item.physical_book.book.title %></p>
        <p>£<%= basket_item.physical_book.price %></p>
      </div>
    </div>
    <hr>
  <% end %>
</div>
<div class="d-flex m-2">
  <%= button_to "Continue Shopping", books_path, method: :get, class: 'btn btn-success' %>
  <%= button_to "Go to checkout", cart_path(@current_cart), method: :get, class: 'btn btn-warning' %>
</div>
